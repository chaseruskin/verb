name: pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: cdotrus/vertex/.github/workflows/build.yml@trunk
  test:
    uses: cdotrus/vertex/.github/workflows/test.yml@trunk