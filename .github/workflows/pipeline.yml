name: Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: chaseruskin/verb/.github/workflows/build.yml@trunk
  # test:
  #   uses: chaseruskin/verb/.github/workflows/test.yml@trunk